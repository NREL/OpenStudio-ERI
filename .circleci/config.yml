version: 2
jobs:
  build:
    docker:
      - image: nrel/openstudio:2.8.0
    steps:
      - checkout
      - run: rm -f Gemfile.lock && bundle install
      - run: rake test:all
      - store_artifacts:
          path: workflow/tests/test_results
          destination: results